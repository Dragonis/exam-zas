{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Zamowienia</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nazwatowaru</th>
                <td>{{ entity.nazwaTowaru }}</td>
            </tr>
            <tr>
                <th>Ilosc</th>
                <td>{{ entity.ilosc }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_zamowienia') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('admin_zamowienia_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
