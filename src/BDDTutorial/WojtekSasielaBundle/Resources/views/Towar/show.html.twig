{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Towar</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nazwa</th>
                <td>{{ entity.nazwa }}</td>
            </tr>
            <tr>
                <th>Cenajednostkowa</th>
                <td>{{ entity.cenaJednostkowa }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_towar') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('admin_towar_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
